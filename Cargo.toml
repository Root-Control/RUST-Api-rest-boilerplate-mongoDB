[package]
name = "api_rest"
version = "0.1.0"
edition = "2018"

# Dependencias para tu proyecto aquí
[dependencies]
warp = "0.3"
tokio = { version = "1", features = ["full"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
